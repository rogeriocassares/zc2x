# applications/gateways

Placeholder for a future application-entry-point layer (per
`.github/copilot-instructions.md`'s repository organization: `applications/`
= firmware entry points). Not populated yet — unlike `obu`/`rsu`, there is no
corresponding firmware or code for a "gateway" role anywhere else in the
repository yet.
